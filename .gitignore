# OS / editor
.DS_Store
.vscode/
.idea/

# Local environment and secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/
bizinsight/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints/

# Streamlit local settings
.streamlit/secrets.toml

# Django / app generated files
staticfiles/
media/
src/django/mysite/static/wordcloud_images/

# Generated local RAG indexes and caches
data/vectorstore/

# 파생 데이터. 원본 CSV에서 재생성한다(build_all / refresh_dynamic_data).
# 커밋하면 원본과 어긋난 채 굳고 빌더가 깨져도 알 수 없다. CI가 매번 재생성한다.
# data/demo/ 아래의 큐레이션된 데모 스냅샷은 추적 대상이므로 영향받지 않는다.
data/canonical/
data/dynamic/

# 포트폴리오 슬라이드 생성물
artifacts/
outputs/

# Internal development notes not intended for public README/repo docs
archive/

# Eval 실행 결과 (재현 가능하므로 커밋하지 않음)
evals/results/*.json
